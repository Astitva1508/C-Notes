void theory();


void theory(){
    // Any edge in a graph, on whose removal,the graph is broken down into two or more components, that edge is called a bridge

    // Use of two arrays:
        // in[] which gives the DFS time of insertion of the given node
        // low[] which gives the lowest time of insertion of all the adjacent nodes apart from parent 
}