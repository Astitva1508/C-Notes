#include <iostream>
using std::string;
using std::end;
using std::cout;

int main(){

    return 0;
}

//Arrays store data in continous chunks of memory

// Disadvantages of C++ Arrays
    // We cannot dynamically create arrays.We cannot add element to an array

//Advatanges
    // Accesing and finding elements is faster