#include <iostream>
using namespace std;

int main(){
    return 0;
}

// Four Parts:
// Containers, Iterators, Functors, Algorithms

// Containers are Data Strucures pre-implemented in C++.
// Classified into Sequential=>Array-esque where values are stores in continous blocks(Vectors,Stack,Queue) , Ordered=>where values are stores in stored order(Map,Multimap,Set,MultiSet), Unordered=>Based on Hashing Algorithms(UnorderedMap,UnOrderedSet) , and other Classes(Pair)

// Iterators (like pointers) point to the memory address of containers
// Declaration of an iterator : vector<int> ::iterator it

// Functors are classes which act as functions